# 03 - 堆疊 Stack

> 語言：Python
> 收錄題號：20、1047、682、155、844、232、496、739
> 本篇是「先想清楚再寫」的學習筆記，程式碼都經過 LeetCode Submit 通過。

---

## 什麼時候該想到 Stack

想像你在洗碗。洗好的盤子一個一個往上疊，要拿的時候，一定是從**最上面**那個開始拿——你不可能從中間抽一個出來。這就是 Stack，**後進先出（LIFO, Last In First Out）**。

再想一個更貼近的：瀏覽器的「上一頁」。你點了 A → B → C，按上一頁回到的是 **C 的前一個 B**，不是最早的 A。因為「最近發生的事」永遠是第一個被回溯的。

**核心直覺一句話：**

> 當「現在該處理誰」取決於「**最近**發生了什麼」，就該想到 Stack。

### 代價表格

| 買到什麼 | 付出什麼 |
|---|---|
| push / pop / peek 全部 O(1)，超快 | 只能碰**尾端**，中間的元素完全摸不到 |
| 天然記住「最近的狀態」，不用自己維護 | 需要 O(n) 額外空間放堆疊 |
| 把 O(n²) 的重複掃描壓成 O(n) | 只能單向走一次，不能反悔重來 |

### 判斷訊號（看到這些字眼就該想到 Stack）

- **括號、配對、匹配、合法性**（左右要成對消除）
- **相鄰、相消、抵銷**（兩個碰在一起就一起消失）
- **回到上一步、撤銷、undo、backspace**
- **下一個比較大／比較小的元素**（Next Greater / Next Smaller）
- **等多久才會遇到 ⋯⋯**（本質同上，只是答案問距離）

### 反向判斷（何時**不要**用 Stack）

- 需要**隨機存取**中間元素 → 用 list 或 dict
- 需要**先進先出**（排隊叫號、廣度優先） → 用 Queue
- 只是要**存起來之後查有沒有** → 用 set 或 dict
- 只是要**從兩端往中間夾** → 用雙指標，不用額外空間

---

## Stack 的四種子模型

| 子模型 | 堆疊裡放什麼 | 什麼時候 pop | 代表題 |
|---|---|---|---|
| **配對消除型** | 還沒被消掉的元素 | 新來的與頂端「成對」時 | 20、1047、844 |
| **歷史紀錄型** | 至今為止有效的紀錄 | 遇到「撤銷」指令時 | 682 |
| **輔助堆疊型** | 主堆疊的「附加資訊」 | 跟著主堆疊同步 | 155、232 |
| **單調堆疊型** | 還沒找到答案、正在排隊等的元素 | 新來的比頂端**大／小**時 | 496、739 |

> **選擇判準：** 先問「**什麼事件會讓堆疊變短**」。是配對？是撤銷？還是「終於遇到更大的了」？答案決定你用哪一個模型。

---

## 常見陷阱（自己踩過的）

1. **忘記檢查堆疊是不是空的就 pop**
   `stack.pop()` 對空堆疊會直接噴 `IndexError`。
   ✅ 一律寫 `while stack and ⋯` 或 `if stack:`，把 `stack` 放在條件式**最前面**（Python 的 `and` 是短路求值，前面是 False 就不會去碰後面）。

2. **迴圈跑完忘記檢查堆疊還有沒有剩東西**
   題目 20：`s = "((("` 全程都沒 pop 過，迴圈結束時堆疊還有 3 個左括號 → 這是**不合法**的。
   ✅ 最後要 `return not stack`，不能只在迴圈裡判斷。

3. **用字串相加來組結果**
   `result += char` 每次都會建一個**全新的字串**，n 次就是 O(n²)。
   ✅ 先把字元 append 進 list，最後 `"".join(stack)`，只有 O(n)。

4. **用 `isdigit()` 判斷是不是數字**
   `"-5".isdigit()` 回傳 **False**，因為負號不是數字字元。
   ✅ 改用「排除法」：先把已知的指令（`+`、`D`、`C`）用 `if / elif` 擋掉，剩下的 `else` 就是數字。

5. **`else` 只綁定最近的那個 `if`**
   多重條件時寫成一串獨立的 `if`，最後的 `else` 只會對應到最後一個 `if`，前面的判斷會全部漏掉。
   ✅ 一組互斥條件要寫成 `if / elif / elif / else` 鏈。

6. **單調堆疊裡存「值」而不是「index」**
   要算「距離」的時候，光有值算不出來——`76 - 75 = 1` 是溫度差，不是天數差。
   ✅ 存 index，需要值的時候用 `temperatures[stack[-1]]` 查表。index 是鑰匙，值隨時查得到。

7. **答案填錯格子**
   單調堆疊 pop 出來時，答案屬於**被彈出的那個人**，不是「現在這個人」。
   ✅ `answer[pop_index] = ⋯`，不是 `answer[index] = ⋯`。

---

# 逐題筆記

---

## 20. Valid Parentheses（有效的括號）

給一串只含 `()[]{}` 的字串，判斷所有括號是否正確配對且順序合法。

**思路**

括號的規則是「**最近打開的，要最先關掉**」——這正是 LIFO。

生活比喻：你在包禮物，先套外層大盒子、再套中層、再套小盒子。拆的時候一定是**從最裡面（最後裝的）先拆**。如果有人叫你先拆最外層，那就是包裝順序錯了。

所以：遇到左括號就丟進堆疊「掛號」，遇到右括號就檢查堆疊頂端是不是它的另一半。

```python
class Solution:
    def isValid(self, s: str) -> bool:
        stack = []
        pairs = {')': '(', ']': '[', '}': '{'}    # 右括號 → 對應的左括號

        for char in s:
            if char in pairs:                     # 是右括號 → 要來配對了
                if not stack or stack.pop() != pairs[char]:
                    return False                  # 沒東西可配 or 配錯人 → 直接失敗
            else:                                 # 是左括號 → 排隊等人來配
                stack.append(char)

        return not stack    # 全部配完才算合法，有剩代表有左括號沒關到
```

**為什麼這樣寫／為什麼不能那樣寫**

- 字典的 key 設成**右括號**，是因為只有遇到右括號才需要查「我該配誰」。反過來設會多繞一圈。
- `not stack or stack.pop() != ⋯` 順序不能顛倒。`or` 是短路求值，`not stack` 先擋掉空堆疊，後面的 `pop()` 才安全。
- 不能只用「計數」來解（左括號 +1、右括號 −1）。`([)]` 數量完全平衡，但順序是錯的——**計數看不出順序，堆疊看得出**。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n) | 每個字元只看一次，push/pop 都是 O(1) |
| 空間 | O(n) | 最壞情況（全是左括號）堆疊裝下整串 |

---

## 1047. Remove All Adjacent Duplicates In String（刪除相鄰重複字元）

不斷刪掉字串中「相鄰且相同」的兩個字元，直到刪不動為止。

**思路**

關鍵洞見：**刪掉一對之後，原本不相鄰的兩個字元會變成相鄰**，可能又要再刪一次。

生活比喻：消消樂。你把兩個一樣的方塊消掉，上面的方塊掉下來，可能又跟下面的湊成一對繼續消。

如果每刪一次就重掃整個字串，那是 O(n²)。但用堆疊，新來的字元只要跟**頂端**比一次就好——因為頂端就是「消完之後真正跟它相鄰的那個」。

```python
class Solution:
    def removeDuplicates(self, s: str) -> str:
        stack = []

        for char in s:
            if stack and stack[-1] == char:
                stack.pop()          # 跟上一個倖存者相同 → 兩個一起消失
            else:
                stack.append(char)   # 不同 → 自己也成為倖存者

        return "".join(stack)        # 堆疊裡剩下的就是最終答案
```

**為什麼這樣寫／為什麼不能那樣寫**

- 堆疊裡放的是「**目前為止存活下來的字元**」，不是「看過的所有字元」。這是 Stack 最重要的心態轉換。
- 消掉之後**不用把 char 放進去**，因為它跟頂端同歸於盡了。
- 結果一定要用 `"".join(stack)`。用 `result += char` 會變 O(n²)（陷阱 3）。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n) | 每個字元最多進出堆疊各一次 |
| 空間 | O(n) | 最壞情況（無相鄰重複）堆疊裝下整串 |

---

## 682. Baseball Game（棒球比賽計分）

依序處理一串指令：數字＝得分、`+`＝前兩次得分相加、`D`＝前一次得分翻倍、`C`＝取消前一次得分。最後回傳總分。

**思路**

`C` 就是 **undo**——而 undo 一定是撤銷「最近一筆」，所以堆疊天生適合。

生活比喻：記帳本。每筆帳往下寫，寫錯了就把最後一行劃掉。你不會回頭去劃掉中間某一筆。

```python
class Solution:
    def calPoints(self, operations: List[str]) -> int:
        stack = []    # 存目前有效的每一筆得分

        for op in operations:
            if op == '+':
                stack.append(stack[-1] + stack[-2])   # 前兩筆相加，兩筆都保留
            elif op == 'D':
                stack.append(stack[-1] * 2)           # 前一筆翻倍，原本那筆保留
            elif op == 'C':
                stack.pop()                           # 撤銷最近一筆
            else:
                stack.append(int(op))                 # 剩下的一定是分數

        return sum(stack)
```

**為什麼這樣寫／為什麼不能那樣寫**

- `+` 和 `D` 用的是 `stack[-1]`（**偷看**不拿走），只有 `C` 用 `pop()`（**拿走**）。這是 peek 與 pop 的差別。
- 判斷數字用 `else` 而不是 `op.isdigit()`——因為分數可能是負的，`"-5".isdigit()` 是 False（陷阱 4）。
- 一定要用 `if / elif / elif / else` 一條鏈。寫成四個獨立的 `if`，最後的 `else` 只會綁到最後一個 `if`（陷阱 5）。
- 因為 `C` 只會撤銷最近一筆，所以堆疊裡留下來的**每一筆都是有效分數**，最後直接 `sum()` 就好。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n) | 掃一次指令 + 最後 sum 一次，相加仍是 O(n) |
| 空間 | O(n) | 最多存下所有得分 |

---

## 155. Min Stack（最小堆疊）

設計一個堆疊，除了 push / pop / top，還要能用 **O(1)** 時間拿到目前的最小值。

**思路**

先想為什麼笨方法不行：

- `min(self.stack)` → 每次都要掃過整個堆疊，是 **O(n)**，違反題目要求。
- 只記一個 `self.min` 變數 → 當這個最小值被 pop 掉，你**不知道第二小的是誰**，就崩了。

關鍵洞見：**最小值也有歷史**。既然主堆疊會回溯，最小值也該跟著回溯。

生活比喻：登山記錄。你一路上山，每經過一個點就記下「**從起點到這裡為止的最低點**」。往回走的時候，把最近那筆記錄撕掉，前一筆自然就是正確答案了。

```python
class MinStack:
    def __init__(self):
        self.stack = []        # 主堆疊：實際的資料
        self.min_stack = []    # 輔助堆疊：與主堆疊同步的「至今最小值」

    def push(self, val: int) -> None:
        self.stack.append(val)
        # 空的、或比目前最小值還小（含相等）→ 更新最小值紀錄
        if not self.min_stack or val <= self.min_stack[-1]:
            self.min_stack.append(val)

    def pop(self) -> None:
        val = self.stack.pop()
        if val == self.min_stack[-1]:    # 走掉的剛好是最小值 → 紀錄也要退一格
            self.min_stack.pop()

    def top(self) -> int:
        return self.stack[-1]

    def getMin(self) -> int:
        return self.min_stack[-1]        # 頂端永遠是目前的最小值，O(1)
```

**為什麼這樣寫／為什麼不能那樣寫**

- `val <= self.min_stack[-1]` 的 **等號不能省**。如果有兩個 3，只記一次，第一個 3 被 pop 掉時紀錄就跟著消失，但堆疊裡其實還有一個 3——最小值就錯了。
- `getMin()` 之所以是 O(1)，是因為答案**早就在 push 的時候算好了**，查詢時只是去頂端拿。這是用「push 時多做一點事」換「查詢時零成本」。
- 這題示範了一個重要模式：**當某個查詢太慢，就在寫入時預先維護好答案**。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(1) | 每個操作都只碰堆疊尾端 |
| 空間 | O(n) | 最壞情況（遞減輸入）輔助堆疊也會裝滿 |

---

## 844. Backspace String Compare（含退格的字串比較）

兩個字串，`#` 代表按一次 backspace。判斷實際打出來的結果是否相同。

**思路**

`#` 就是「刪掉**最近**打的那個字」——又是 LIFO。

生活比喻：在手機上打字。按倒退鍵刪掉的一定是最後打的那個字，而且字都刪光了還一直按，也不會出事（沒東西可刪就算了）。

先把兩個字串各自「還原成真正打出來的樣子」，再直接比較。

```python
class Solution:
    def backspaceCompare(self, s: str, t: str) -> bool:
        def build(string: str) -> str:
            stack = []
            for char in string:
                if char == '#':
                    if stack:          # 有東西才刪，空的就當作沒事發生
                        stack.pop()
                else:
                    stack.append(char)
            return "".join(stack)

        return build(s) == build(t)
```

**為什麼這樣寫／為什麼不能那樣寫**

- `if stack:` 這層檢查不能省。`"#a"` 開頭就按倒退鍵，沒檢查就會 `IndexError`（陷阱 1）。
- 抽出 `build()` 這個內部函式，是因為 s 和 t 要做**完全一樣的事**——重複的邏輯就該包成函式。
- 不能「邊比邊走」直接一對一比對，因為 `#` 會讓兩邊的長度隨時對不齊，位置無法對應。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n + m) | 兩個字串各掃一次 |
| 空間 | O(n + m) | 兩個堆疊 |

> **待補（follow-up）：** 進階版要求 O(1) 空間。思路是從**兩個字串的尾端**往前走，遇到 `#` 就累計「欠幾個字要跳過」，用雙指標邊走邊比，不建任何堆疊。目前只推導出思路，尚未實作。

---

## 232. Implement Queue using Stacks（用堆疊實作佇列）

只能用堆疊（LIFO）的操作，做出一個佇列（FIFO）的行為。

**思路**

看起來矛盾：一個後進先出，一個先進先出。但**倒兩次就變回來了**。

生活比喻：一疊撲克牌。你想拿最底下那張，可以把整疊一張一張翻到旁邊——原本在底下的就跑到新那疊的最上面了。翻兩次 = 順序反轉兩次 = 恢復原狀。

```python
class MyQueue:
    def __init__(self):
        self.in_stack = []     # 專門負責「進來」
        self.out_stack = []    # 專門負責「出去」（順序已經反轉好）

    def push(self, x: int) -> None:
        self.in_stack.append(x)

    def pop(self) -> int:
        self.peek()                    # 先確保 out_stack 有貨
        return self.out_stack.pop()

    def peek(self) -> int:
        if not self.out_stack:         # 只有出貨區空了，才整批搬過去
            while self.in_stack:
                self.out_stack.append(self.in_stack.pop())
        return self.out_stack[-1]

    def empty(self) -> bool:
        return not self.in_stack and not self.out_stack
```

**為什麼這樣寫／為什麼不能那樣寫**

- **關鍵是 `if not self.out_stack:` 這個條件。** 只有出貨區清空時才搬運。如果每次 pop 都搬一次，會退化成 O(n)。
- 為什麼不能只用一個堆疊、每次都倒過來？因為那樣每個操作都是 O(n)，總共 O(n²)。
- **時間複雜度是攤還 O(1)**：某一次 pop 可能要搬 n 個很貴，但每個元素一生只會被搬**一次**（in → out），總搬運量有上限。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | 攤還 O(1) | 每個元素最多被搬運一次 |
| 空間 | O(n) | 兩個堆疊加起來最多 n 個元素 |

---

## 496. Next Greater Element I（下一個更大元素 I）

對 `nums1` 中的每個數字，在 `nums2` 裡找到它的位置，並回傳它**右邊第一個比它大**的數字；沒有就回 −1。

**思路**

暴力法是雙層迴圈 O(n×m)，而且**同一段區間被重複掃描很多次**——這種浪費就是優化的訊號。

關鍵洞見：把 nums2 掃**一次**，就一口氣把所有數字的答案都算出來，存進字典備查。

生活比喻：排隊等身高更高的人。每個人進場時先站著排隊，新來的人如果比隊伍中某些人高，那些人就「等到了」，可以離場登記答案。因為隊伍裡永遠是**由高到矮**排列（矮的一來就把更矮的擠走了）。

```python
class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        next_greater = {}    # 數字 → 它右邊第一個更大的數字
        stack = []           # 單調遞減堆疊：還在等「更大的人」出現的數字

        for num in nums2:
            while stack and stack[-1] < num:
                next_greater[stack.pop()] = num    # 等到了！登記答案後離場
            stack.append(num)                      # 自己也開始排隊等

        # 查表；沒登記到的代表右邊沒有更大的 → -1
        return [next_greater.get(n, -1) for n in nums1]
```

**為什麼這樣寫／為什麼不能那樣寫**

- 用 `while` 而不是 `if`。一個大數字可能一次解救**好幾個**排隊的人，要全部清完。
- 堆疊會維持**由底到頂遞減**，因為新來的一定會把所有比它小的擠出去。這就是「單調堆疊」名字的由來。
- `dict.get(n, -1)` 一行就處理掉「查無此人」的情況，不用另外寫 if。
- 這題可以存值不存 index，是因為題目保證**數字不重複**，且答案要的是值。下一題 739 就不行了。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n + m) | nums2 掃一次、nums1 查一次，是**相加**不是相乘 |
| 空間 | O(n) | 字典 + 堆疊 |

---

## 739. Daily Temperatures（每日溫度）｜ Medium

給每天的溫度，回傳每一天「還要等幾天才會遇到更溫暖的一天」，等不到就填 0。

**思路**

跟 496 是**同一個工具、不同的問法**：496 問「那個更大的數字**是什麼**」，739 問「隔了**幾天**」。

問距離，就代表要做減法；要做減法，堆疊裡就必須存 **index** 而不是溫度——`76 − 75 = 1` 是溫度差，`6 − 2 = 4` 才是天數差。

生活比喻：借書單。單子上只寫「第 3 排第 5 格」，需要知道是哪本書的時候，再拿位置去架上看一眼。不用把整本書抄下來。

```python
class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:
        answer = [0] * len(temperatures)    # 預設 0：等不到更熱的日子就維持 0，不用另外處理
        stack = []                          # 存「還在等答案」的 index

        for index, t in enumerate(temperatures):
            # 今天比排隊中那幾天都熱 → 他們的答案揭曉了
            while stack and temperatures[stack[-1]] < t:
                pop_index = stack.pop()
                answer[pop_index] = index - pop_index    # 答案屬於「等待的那天」，不是今天

            stack.append(index)

        return answer
```

**為什麼這樣寫／為什麼不能那樣寫**

- **堆疊存 index，不存溫度。** index 是鑰匙，用 `temperatures[stack[-1]]` 隨時能查到溫度；反過來從溫度找 index 會被重複值坑死。
- **不需要兩個堆疊。** index 由下往上遞增、溫度由下往上遞減，兩件事在同一個堆疊裡同時成立，不衝突。
- `answer[pop_index]` 而不是 `answer[index]`。答案屬於**被解救的那個人**，今天這個人自己的答案還沒揭曉（陷阱 7）。
- `answer` 預設填 0，讓「永遠等不到」的情況**自動正確**，迴圈結束後不用再處理剩下的堆疊。

| 複雜度 | 值 | 說明 |
|---|---|---|
| 時間 | O(n) | 雖然是巢狀迴圈，但每個 index 進出堆疊各**一次**，總操作量 2n |
| 空間 | O(n) | 最壞情況（溫度一路遞減）堆疊裝下全部 index |

> **關於「巢狀迴圈不一定是 O(n²)」：**
> 不要只盯著「某一圈跑幾次」，要看「**總共**跑幾次」。就像你發出 n 張號碼牌再陸續收回——某一次可能一口氣收 5 張，但總量永遠不會超過 n。這種分析法叫 **攤還分析（Amortized Analysis）**。

---

# 系列總結

| 題號 | 用的結構或模型 | 關鍵洞見 |
|---|---|---|
| 20 | 配對消除型 | 計數只看得出數量，堆疊才看得出**順序** |
| 1047 | 配對消除型 | 堆疊存的是「**倖存者**」，不是「看過的所有東西」 |
| 682 | 歷史紀錄型 | `C` 就是 undo；peek（`[-1]`）與 pop 是兩回事 |
| 155 | 輔助堆疊型 | 查詢太慢，就在**寫入時**先把答案算好 |
| 844 | 配對消除型 | 兩邊各自還原成最終樣貌再比，不要邊走邊比 |
| 232 | 輔助堆疊型 | 反轉兩次 = 沒反轉；「懶惰搬運」換來攤還 O(1) |
| 496 | 單調堆疊型 | 掃一次就把所有答案算完，存字典備查 |
| 739 | 單調堆疊型 | 要算距離就存 **index**；巢狀迴圈也可能是 O(n) |

---

## 一句話帶走

> **當「現在該處理誰」取決於「最近發生了什麼」，就用 Stack；當你發現自己在重複掃描右邊找第一個更大的，就用單調堆疊。**

---

## 與其他技巧的分工對照

| 技巧 | 核心問題 | 典型訊號 | 空間 |
|---|---|---|---|
| **Hash Map** | 「我看過這個嗎？出現幾次？」 | 重複、計數、配對查找、兩數之和 | O(n) |
| **Two Pointers** | 「從兩端往中間夾，或快慢兩個指標」 | 已排序、回文、原地修改、移除元素 | O(1) |
| **Stack** | 「最近發生的事決定現在怎麼做」 | 括號、相消、undo、下一個更大 | O(n) |

**三者最容易混淆的地方：**

- Hash Map 和 Stack 都能「記住看過的東西」，差別在 Hash Map 是**無序查找**，Stack 在意**先後順序**。
- Two Pointers 和 Stack 都能把 O(n²) 壓成 O(n)，差別在 Two Pointers 要求資料**已排序或有對稱性**且不花額外空間，Stack 不要求排序但要付 O(n) 空間。
